#!/usr/bin/env bash
#this is an exercise in while loops 

i=-1
h=0
while [ $i -le 60 ]
do
	if [ $i -eq 60 ] 
	then 
		
		h=$((h+1))
		if [ $h -eq 13 ]
		then
			h=0
		fi
		echo "Hour: $h"
		i=0
	elif [ $i -eq -1 ]
	then 
		echo "Hour: $h"
		i=0
	
	else 
		echo "$i"
	fi
 	
	i=$((i+1))
	
	 
done
 
